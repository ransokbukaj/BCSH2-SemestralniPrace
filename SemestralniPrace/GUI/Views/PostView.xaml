<UserControl x:Class="GUI.Views.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">

        <!-- Stejná struktura jako ostatní view -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Hlavička -->
            <RowDefinition Height="*"/>
            <!-- Obsah -->
            <RowDefinition Height="Auto"/>
            <!-- CRUD tlačítka -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <!-- seznam pošt -->
            <ColumnDefinition Width="6*"/>
            <!-- detail pošty -->
        </Grid.ColumnDefinitions>

        <!-- Hlavička -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Pošty"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- LEVÁ ČÁST – seznam pošt -->
        <DataGrid Grid.Row="1" Grid.Column="0"
                  ItemsSource="{Binding Posts}"
                  SelectedItem="{Binding SelectedPost, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,10,0">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>-->
                <DataGridTextColumn Header="Město" Binding="{Binding City}" Width="*"/>
                <DataGridTextColumn Header="PSČ" Binding="{Binding PSC}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- PRAVÁ ČÁST – detail pošty ve ScrollVieweru -->
        <ScrollViewer Grid.Row="1" Grid.Column="1"
                      Margin="10,0,0,0"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Detail pošty"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <!--<StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="ID:" Width="120"/>
                    <TextBlock Text="{Binding SelectedPost.Id}"/>
                </StackPanel>-->

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Město:" Width="120"/>
                    <TextBox Text="{Binding SelectedPost.City, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="PSČ:" Width="120"/>
                    <TextBox Text="{Binding SelectedPost.PSC, UpdateSourceTrigger=PropertyChanged}"
                             Width="100"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- SPODNÍ TLAČÍTKA – sjednocená poloha -->
        <StackPanel Grid.Row="2" Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Nový"
                    Command="{Binding NewCommand}"
                    Margin="0,0,10,0"
                    Width="80"/>
            <Button Content="Uložit"
                    Command="{Binding SaveCommand}"
                    Margin="0,0,10,0"
                    Width="80"/>
            <Button Content="Smazat"
                    Command="{Binding DeleteCommand}"
                    Width="80"/>
        </StackPanel>

    </Grid>
</UserControl>
