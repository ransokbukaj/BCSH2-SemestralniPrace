<UserControl x:Class="GUI.Views.EducationProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
              Background="White"
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Vzdělávací programy"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" Grid.Column="0"
                  ItemsSource="{Binding EducationPrograms}"
                  SelectedItem="{Binding SelectedEducationProgram, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Od" Binding="{Binding From, StringFormat=\{0:d\}}" Width="*"/>
                <DataGridTextColumn Header="Do" Binding="{Binding To, StringFormat=\{0:d\}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Row="1" Grid.Column="1" Margin="10,0,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Detail programu" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Název:" Width="140"/>
                    <TextBox Text="{Binding SelectedEducationProgram.Name, UpdateSourceTrigger=PropertyChanged}" Width="250"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBlock Text="Od:" Width="140"/>
                    <DatePicker SelectedDate="{Binding SelectedEducationProgram.From, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Do:" Width="140"/>
                    <DatePicker SelectedDate="{Binding SelectedEducationProgram.To, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                </StackPanel>

                <TextBlock Text="Popis:" Margin="0,5,0,5"/>
                <TextBox Text="{Binding SelectedEducationProgram.Description, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Height="80"
                         VerticalScrollBarVisibility="Auto"/>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="0,15,0,0">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Nový" Command="{Binding NewCommand}" Margin="0,0,10,0" Width="80"/>
                        <Button Content="Uložit" Command="{Binding SaveCommand}" Margin="0,0,10,0" Width="80"/>
                        <Button Content="Smazat" Command="{Binding DeleteCommand}" Width="80"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Přidej výstavu" Command="{Binding LoadCommand}" Margin="0,0,10,0" Width="120"/>
                        <Button Content="Odeber výstavu" Command="{Binding LoadCommand}" Width="120"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>